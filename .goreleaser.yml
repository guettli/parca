before:
  hooks:
    - go mod tidy
    - make ui/build
builds:
  - main: ./cmd/parca/
    id: "parca"
    binary: parca
    # https://goreleaser.com/customization/build/#reproducible-builds
    mod_timestamp: "{{ .CommitTimestamp }}"
    # The embedded DuckDB storage backend (github.com/marcboeker/go-duckdb)
    # links a static libduckdb through cgo, so the binary MUST be built with
    # CGO_ENABLED=1 and a C/C++ toolchain. cgo cross-compilation with the
    # prebuilt libduckdb is impractical, so this single-node build targets
    # linux/amd64 only (a CGO_ENABLED=0 build fails to link the
    # duckdb-go-bindings, e.g. "undefined: bindings.Type"). See
    # Dockerfile.duckdb / docs/duckdb-image.md for the same gotchas.
    env:
      - CGO_ENABLED=1
    goos:
      - linux
    goarch:
      - amd64
    flags:
      - -trimpath
      - -v
    ldflags:
      # Default is `-s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}} -X main.builtBy=goreleaser`.
      # -static-libstdc++/-static-libgcc fold the C++ runtime into the binary so
      # it only depends on glibc at runtime (matches Dockerfile.duckdb).
      - -linkmode external
      - -extldflags '-static-libstdc++ -static-libgcc'
      - -X main.version={{.Version}} -X main.commit={{.Commit}}
archives:
    # e.g. parca_0.15.0_Darwin_arm64.tar.gz, parca_0.15.0_Darwin_x86_64.tar.gz, parca_0.15.0_Linux_arm64.tar.gz, parca_0.15.0_Linux_x86_64.tar.gz
  - name_template: >-
      {{ .ProjectName }}_
      {{- trimprefix .Version "v" }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip
snapcrafts:
  - name: parca
    # Handle publishing with snapcraft itself
    publish: false
    summary: Parca continuous profiling tool
    description: |
      Continuous profiling for analysis of CPU and memory usage, down to the line
      number and throughout time. Saving infrastructure cost, improving performance,
      and increasing reliability.
    grade: stable
    confinement: strict
    license: Apache-2.0
    base: core22
    extra_files:
      - source: snap/parca-wrapper
        destination: bin/parca-wrapper
        mode: 0755
      - source: snap/example-config.yaml
        destination: usr/share/parca/example-config.yaml
        mode: 0700
      - source: snap/hooks/configure
        destination: snap/hooks/configure
        mode: 0755
      - source: snap/hooks/configure
        destination: meta/hooks/configure
        mode: 0755
    apps:
      parca:
        command: parca
        plugs:
          - network-bind
          - network
          - home
          - etc-parca
      parca-svc:
        command: bin/parca-wrapper
        daemon: simple
        install_mode: disable
        restart_condition: never
        plugs:
          - network-bind
          - network
          - home
          - etc-parca
    plugs:
      etc-parca:
        interface: system-files
        read:
          - /etc/parca
checksum:
  name_template: "checksums.txt"
snapshot:
  name_template: "{{ incpatch .Tag }}-{{ .ShortCommit }}"
source:
  enabled: true
release:
  prerelease: auto
  # Defaults to empty.
  footer: |
    ## Docker images

    `docker pull ghcr.io/parca-dev/parca:{{ .Tag }}`

    ## Thanks!

    Join our [Discord server](https://discord.com/invite/ZgUpYgpzXy);
    Follow us on [Twitter](https://twitter.com/ParcaDev);
    Read the [documentation](https://www.parca.dev/docs/overview).
changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
